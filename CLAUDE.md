# Homebrew Tap

Homebrew tap for lintro.

## Commands

- `brew install lgtm-hq/tap/lintro` — Install lintro via Homebrew
- `brew audit --strict Formula/*.rb` — Audit formulas
- `brew test lintro` — Run formula tests

## Stack

- Ruby (Homebrew formulas)
- Shell scripts for CI validation