# typed: strict
# frozen_string_literal: true

# Homebrew formula for {{FORMULA_KEY}}
# CLI tools are installed as Homebrew dependencies; Python libraries are bundled
class {{CLASS_NAME}} < Formula
  include Language::Python::Virtualenv

  desc "{{DESCRIPTION}}"
  homepage "{{HOMEPAGE}}"
  url "{{TARBALL_URL}}"
  sha256 "{{TARBALL_SHA}}"
  license "{{LICENSE}}"{{HEAD_BLOCK}}{{BOTTLE_COMMENT_BLOCK}}

  livecheck do
    url :stable
    strategy :pypi
  end

  # CLI tools installed via Homebrew
{{HOMEBREW_DEPS}}{{CONFLICTS_BLOCK}}

  # Pure Python library dependencies
{{POET_RESOURCES}}
{{WHEEL_RESOURCES}}
  def install
    venv = virtualenv_create(libexec, "{{PYTHON_VENV}}")

{{INSTALL_RESOURCES}}

    # Install the package itself
    venv.pip_install_and_link buildpath
  end
{{CAVEATS_BLOCK}}
  test do
    assert_match version.to_s, shell_output("#{bin}/{{TEST_BINARY}} --version"){{TEST_EXTRA_BLOCK}}
  end
end
